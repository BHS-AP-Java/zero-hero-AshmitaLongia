package edu.bhscs;

interface TestInterface {
  // fields and properties

  class Cake implements TestInterface {}
}

/*
 *
 * for (int i = 0; i < width; i++) { System.out.print(top); } System.out.println();
 *
 * for (int row = 0; row < 1; row++) { // horizontal distance between "leg" for (int col = 0; col <
 * width; col++) {
 *
 * if (col % (width / (legs - 1)) == 0) { // checks if the col is divisible by the interval
 * System.out.print(leg) ; // if the remainder is 0, the col is a multiple of the interval
 *
 *
 * } else { System.out.print(" "); } } System.out.println(); } }
 */
